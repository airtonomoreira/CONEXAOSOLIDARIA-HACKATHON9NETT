name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'

    - name: Restore dependencies
      run: dotnet restore ConexaoSolidaria.sln

    - name: Build solution
      run: dotnet build ConexaoSolidaria.sln --configuration Release --no-restore

    - name: Run tests
      run: dotnet test ConexaoSolidaria.sln --configuration Release --no-build --verbosity normal

    - name: Build Docker images
      run: |
        docker build -f docker/AuthService.Dockerfile -t AIRTONMOREIRA/auth-service:${{ github.sha }} .
        docker build -f docker/CampaignService.Dockerfile -t AIRTONMOREIRA/campaign-service:${{ github.sha }} .
        docker build -f docker/DonationService.Dockerfile -t AIRTONMOREIRA/donation-service:${{ github.sha }} .
        docker build -f docker/DonationWorker.Dockerfile -t AIRTONMOREIRA/donation-worker:${{ github.sha }} .
        docker build -f docker/TransparencyService.Dockerfile -t AIRTONMOREIRA/transparency-service:${{ github.sha }} .

    - name: Login to Docker Hub
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Push Docker images
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        docker tag AIRTONMOREIRA/auth-service:${{ github.sha }} AIRTONMOREIRA/auth-service:latest
        docker tag AIRTONMOREIRA/campaign-service:${{ github.sha }} AIRTONMOREIRA/campaign-service:latest
        docker tag AIRTONMOREIRA/donation-service:${{ github.sha }} AIRTONMOREIRA/donation-service:latest
        docker tag AIRTONMOREIRA/donation-worker:${{ github.sha }} AIRTONMOREIRA/donation-worker:latest
        docker tag AIRTONMOREIRA/transparency-service:${{ github.sha }} AIRTONMOREIRA/transparency-service:latest
        
        docker push AIRTONMOREIRA/auth-service:${{ github.sha }}
        docker push AIRTONMOREIRA/auth-service:latest
        docker push AIRTONMOREIRA/campaign-service:${{ github.sha }}
        docker push AIRTONMOREIRA/campaign-service:latest
        docker push AIRTONMOREIRA/donation-service:${{ github.sha }}
        docker push AIRTONMOREIRA/donation-service:latest
        docker push AIRTONMOREIRA/donation-worker:${{ github.sha }}
        docker push AIRTONMOREIRA/donation-worker:latest
        docker push AIRTONMOREIRA/transparency-service:${{ github.sha }}
        docker push AIRTONMOREIRA/transparency-service:latest
