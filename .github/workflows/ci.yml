name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'

    - name: Restore dependencies
      run: dotnet restore ConexaoSolidaria.sln

    - name: Build solution
      run: dotnet build ConexaoSolidaria.sln --configuration Release --no-restore

    - name: Run tests
      run: dotnet test ConexaoSolidaria.sln --configuration Release --no-build --verbosity normal

    - name: Build Docker images
      run: |
        docker build -f docker/AuthService.Dockerfile -t docker.io/AIRTONMOREIRA/auth-service:${{ github.sha }} .
        docker build -f docker/CampaignService.Dockerfile -t docker.io/AIRTONMOREIRA/campaign-service:${{ github.sha }} .
        docker build -f docker/DonationService.Dockerfile -t docker.io/AIRTONMOREIRA/donation-service:${{ github.sha }} .
        docker build -f docker/DonationWorker.Dockerfile -t docker.io/AIRTONMOREIRA/donation-worker:${{ github.sha }} .
        docker build -f docker/TransparencyService.Dockerfile -t docker.io/AIRTONMOREIRA/transparency-service:${{ github.sha }} .

    - name: Login to Docker Hub
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: docker/login-action@v3
      with:
        username: airton.moreira@gmail.com
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Push Docker images
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        docker tag docker.io/AIRTONMOREIRA/auth-service:${{ github.sha }} docker.io/AIRTONMOREIRA/auth-service:latest
        docker tag docker.io/AIRTONMOREIRA/campaign-service:${{ github.sha }} docker.io/AIRTONMOREIRA/campaign-service:latest
        docker tag docker.io/AIRTONMOREIRA/donation-service:${{ github.sha }} docker.io/AIRTONMOREIRA/donation-service:latest
        docker tag docker.io/AIRTONMOREIRA/donation-worker:${{ github.sha }} docker.io/AIRTONMOREIRA/donation-worker:latest
        docker tag docker.io/AIRTONMOREIRA/transparency-service:${{ github.sha }} docker.io/AIRTONMOREIRA/transparency-service:latest
        
        docker push docker.io/AIRTONMOREIRA/auth-service:${{ github.sha }}
        docker push docker.io/AIRTONMOREIRA/auth-service:latest
        docker push docker.io/AIRTONMOREIRA/campaign-service:${{ github.sha }}
        docker push docker.io/AIRTONMOREIRA/campaign-service:latest
        docker push docker.io/AIRTONMOREIRA/donation-service:${{ github.sha }}
        docker push docker.io/AIRTONMOREIRA/donation-service:latest
        docker push docker.io/AIRTONMOREIRA/donation-worker:${{ github.sha }}
        docker push docker.io/AIRTONMOREIRA/donation-worker:latest
        docker push docker.io/AIRTONMOREIRA/transparency-service:${{ github.sha }}
        docker push docker.io/AIRTONMOREIRA/transparency-service:latest
